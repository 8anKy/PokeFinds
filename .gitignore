node_modules/
.next/
# Glob, inte exakt namn: .env.production, .env.local.bak, .env.*.local m.fl. måste
# ALLA fångas — repot är PUBLIKT och en enda oskyddad env-fil läcker prod-hemligheter.
.env*
!.env.example
*.log
coverage/
playwright-report/
test-results/
prisma/migrations/dev/
.DS_Store
.cache/
.scratch/
# Actions-cachade arbetsfiler för restock-lanarna (lagerläge + ruttabell). De skapas
# också lokalt vid torrkörning och hör aldrig hemma i repot.
.restock-cache/
.discord-restock-cache/
.restock-routes/
*.tsbuildinfo
next-env.d.ts
# Glob, inte exakt namn: verktyg lämnar sidofiler (t.ex. .doctor-bak) som annars
# slank förbi regeln och hamnade i det PUBLIKA repot.
.claude/settings.local.json*
.vercel

.vercel
.claude/skills/ponytail/
.claude/skills/ponytail-audit/
.claude/skills/ponytail-debt/
.claude/skills/ponytail-help/
.claude/skills/ponytail-review/
codemagic-cert-key.pem

# Revisionsartefakter (engångskörning, ligger inte i repot)
.audit/

# Spike-artefakter: katalogdump + ~20 000 cachade kortbilder. Repot är PUBLIKT
# och det här är hundratals MB — får aldrig checkas in.
.spike/

# Android-signering — nyckeln och lösenorden får ALDRIG in i det publika repot
android/keystore.properties
*.keystore
*.jks

# IDE-lokalt (Android Studio) + Python-bytecode från skills-skript
.idea/
__pycache__/

# iOS-projektet genereras färskt av Codemagic (`cap add ios`) — checkas aldrig in
ios/

# Katalog-säkerhetskopior (stora, prod-data — hör inte hemma i git)
backup-catalog-*.json

# Juridiskt arbetsmaterial — får ALDRIG in i det publika repot. Dokumenten räknar
# upp OÅTGÄRDADE luckor i villkoren för en LIVE kommersiell tjänst, dvs en färdig
# lista till den som vill bestrida något. De bor i ../PokeFinds-private/docs/.
docs/TERMS-GAP.md
docs/TERMS-DRAFT-CLAUSES.md
