<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- Matcha statusfältet mot appens mörka bakgrund (annars systemets grå). Vita ikoner. -->
        <item name="android:statusBarColor">#0a0a0c</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- Android 12+ systemsplash: mörk bakgrund (#0a0a0c) matchar appen — annars
             visade OS:et en ljus/vit splash före appen. Äldre Android använder
             @drawable/splash (numera mörk + Foilio-märke). postSplashScreenTheme
             lämnar över till app-temat. -->
        <item name="windowSplashScreenBackground">#0a0a0c</item>
        <item name="android:background">@drawable/splash</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>