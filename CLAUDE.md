# Foilio — Pokémon TCG-marknadsplattform för Sverige

## Vad är detta?
En komplett SaaS-webbplattform för svenska Pokémon TCG-samlare: prisbevakning, restock-alerts,
marknadsdata, samlingsvärde, kortskanning och community. Helt eget varumärke ("Foilio"),
egen design, egen copy (svenska). Nämn ALDRIG inspirations-/konkurrentsidor i kod, copy eller docs.

## Status (uppdatera varje session!)
- [x] Projektplan skapad (12 tasks i task-systemet)
- [x] Scaffold: configs, package.json, Docker, .env.example
- [x] Prisma schema (alla modeller) + seed-script (20 sets, 200 kort, 50 sealed, 10 retailers, 500+ prisobservationer, 50 restocks, 20 användare, 100 posts)
- [x] Design system + UI-komponenter (dark theme, neon accents)
- [x] Auth (NextAuth credentials, register/login/forgot/verify, onboarding, RBAC)
- [x] Services + API routes (products, watchlist, alerts, collection, community, admin, market, scanner)
- [x] Scraper/adapter-system (SourceAdapter interface, mock-adapter, normalisering, fuzzy matching, restock detection)
- [x] Notifikationer + e-postmallar (in-app + nodemailer, console-transport i dev)
- [x] Frontend-sidor (landing, dashboard, katalog, produktsida, watchlist, collection, scanner, market, community, admin, pricing, legal)
- [x] Kortskanning MVP (mock OCR-adapter, dokumenterad väg till riktig vision-API)
- [x] SEO/GDPR (metadata, sitemap, robots, cookie-banner, dataexport, kontoradering)
- [x] Tester (vitest unit + Playwright e2e-skelett)
- [x] Dokumentation (README, docs/-mapp)
- [x] Verifierad: `npm install && npx prisma migrate dev && npx prisma db seed && npm run dev`
- [x] Riktig data: seed använder riktiga sets/kort/bilder (statisk snapshot i `prisma/data/real-cards.json`, källa Pokémon TCG API) + riktiga svenska/EU-retailers. `npm run import:tcg` hämtar full färsk kortdata från api.pokemontcg.io (officiellt API, valfri nyckel `POKEMONTCG_API_KEY`) — körd 2026-06-11: 18 set, 3443 kort med bilder, 2947 produkter/prisobservationer
- [x] robots.txt verifierad 2026-06-11 via Firecrawl: Spelexperten/Webhallen/Alphaspel/Dragon's Lair/Spelbutiken/CDON tillåter produktsidor (noterat i `ScrapeSource.config.robots`); Spel & Sånt gick ej att nå (ej verifierad); Amazon/Cardmarket otillåtna enligt villkor. Riktiga skrapade SEK-priser i seed (`REAL_RETAIL_PRICES`, Spelexperten + Dragon's Lair)
- [x] Designomgörning 2026-06-11 (impeccable): Inter-font, solid holo-accent (gradient-text borttagen), eget ikonbibliotek `src/components/ui/icons.tsx` (0 emojis kvar i UI), marketing + app-sidor omdesignade (inga identiska kortgrids/eyebrows/hero-metric), focus-visible + reduced-motion globalt. tsc + 71/71 tester gröna
- [x] Användarguide: `docs/ANVANDARGUIDE.md` (start, test per funktion, demo-konton, felsökning)
- [x] Design v2 2026-06-11: Ny professionell färgpalett (Nordic Professional — deep navy, blue-500 primary, amber gold accent). Holo/neon-värden ersatta med seriösa toner. Motion tillagd (stagger-lists, scale-in modals, slide-in toasts, button press-feedback, hover-transitions). Landing page förenklad (6→4 sektioner, renare hierarki). Reduced-motion respekteras.
- [x] Svenska butiksskrapare: Spelexperten, Webhallen, Dragon's Lair, Alphaspel — riktiga SourceAdapter-implementationer i `src/scrapers/adapters/`. Registrerade i runner.ts. Seed skapar dem som aktiva ScrapeSource. Alla respekterar robots.txt via politeFetch.
- [x] Offer-djuplänkar: Seed genererar nu produkt-specifika URL:er per butik istf. startsida. Skrapade offers skrivs med riktiga produkt-URL:er.
- [x] Launch-fix 2026-06-11: Alla singelkortspriser ombyggda från riktiga Cardmarket trend-priser (PriceObservation, EUR→SEK 11.50). Fabricerade Cardmarket/Tradera-offers på sealed borttagna — sealed har endast riktiga skrapade butikspriser. Alla 5 skrapare lagade (Webhallen JSON-API, Spelexperten iButik-parsning, Dragon's Lair :product-data, Alphaspel, Tradera kategori-browse — Traderas robots.txt förbjuder /search). `scripts/run-scrapers.ts` använder matchProduct (bigram-Dice + produktform-vakt i `src/scrapers/matching.ts`: pack matchar aldrig box/ETB) + kategoribaserat skick → inga dubbletter. Outlier-filter med kategoriband (`scripts/remove-outlier-offers.ts`). 622 nya kort fick bilder. 19 227 dagliga prissnapshots. 71/71 unit + 20/20 sajttester gröna.

- [x] Katalog-komplettering 2026-06-12: 173 set, 20 221 kort (250-korts-cap borttagen — sv10/sm5/swsh8/swshp m.fl. kompletta; me2pt5 har 250 unika kort i API:t trots totalCount 295). `Offer.price` nu nullable (migration `offer_price_nullable`) → länk-offers utan pris tillåtna. ALLA 20 638 produkter har minst Cardmarket + Tradera i butikslistan (`scripts/backfill-marketplace-offers.ts`): singlar med riktigt Cardmarket EN trend-pris, övriga som länk-offer (price=null, UI visar "Se aktuella annonser"). Sealed-produkter (Booster Pack/Box, ETB) skapade för alla huvudset (`scripts/create-sealed-products.ts`, 415 sealed totalt) → set+kategori-filtret ger alltid träffar. Import matchar nu produkt via cardId (inte slug) — 622 slug-dubbletter ihopslagna (`scripts/dedupe-card-products.ts`). 621 singlar saknar pris i API:t → ärliga länk-offers. tsc + 71/71 tester gröna.
- [x] Länk- & prisfix 2026-06-12: (1) **Exakta Cardmarket-länkar**: 18 993 singel-offers djuplänkar nu via `https://prices.pokemontcg.io/cardmarket/{tcgExternalId}` (302 → exakt produktsida); övriga använder namn-sökning (`searchString={kortnamn}&site=1` — namn+set+nummer ger 0 träffar hos CM). Tradera-länkar: `?q=Pokemon {namn/titel}`. URL-byggare delade i `scripts/marketplace-urls.ts`, omskrivning via `scripts/fix-marketplace-urls.ts`. (2) **TCGdex-priser**: 587 tidigare oprissatta singlar fick riktiga Cardmarket trend-priser via TCGdex API (`scripts/import-tcgdex-prices.ts`, källa "TCGdex API") — pokemontcg.io saknar prisdata för nyaste seten (me2pt5/me3/me4). TCGdex har andra set-id:n (matcha via namn + `SET_ID_ALIASES`) och zero-paddade localIds (prova padStart(3,"0")). Endast 37 singlar kvar utan pris (saknar marknadsdata överallt). (3) **Matcher-omskrivning** (`src/scrapers/matching.ts`): distinctiveOverlap ≥ 0.5 (kandidat-täckning av särskiljande ord, stopp-/formord/siffror exkluderade), språkvakt (japansk/kinesisk ≠ EN), per-token kandidat-union, accessory/multipack-former. Sealed↔singel-vakt i runner.ts + run-scrapers.ts. 32 felmatchade offers raderade (`scripts/clean-mismatched-offers.ts`, validerar via URL-slug), omskrapning gav 291 korrekta offers. (4) **8 h-uppdatering**: 30 s-polling borttagen ur `live-product-pricing.tsx` (engångshämtning + ny copy), BullMQ repeat/worker-fallback/instrumentation = 8 h (`SCRAPE_INTERVAL_MINUTES` default 480). tsc + 71/71 tester gröna.

- [x] Cardmarket-prisguide + källhistorik 2026-06-12: (1) **Officiell CM-prisguide** (downloads.s3.cardmarket.com, ingen scraping) prissätter sealed: `scripts/import-cardmarket-priceguide.ts` matchar 328/357 EN-sealed mot CM:s nonsingles-katalog (324 prissatta; 4 vintage-boxar saknar CM-prisdata; sealed-poster i guiden saknar avg1/avg7/avg30 — endast trend), EUR→SEK 11.50, idempotent per prisguide-utgåva (`guideCreatedAt`) så historiken ackumuleras vid återkommande körningar. 46 påhittade sealed-produkter raderade (`scripts/remove-nonexistent-sealed.ts`, CM-katalogen som existens-orakel). (2) **Datasanering**: 19 042 fabricerade Tradera-priser på sök-URL:er nollade till länk-offers, fabricerad CM-frakt nollad, CDON + Amazon.se borttagna helt, GRADED_CARD-artefakter raderade. Seed genererar inte längre syntetisk prishistorik. (3) **Riktig prishistorik per källa**: `scripts/backfill-singles-history.ts` skapade 63 882 historikpunkter för 21 294 singlar från CM:s egna avg1/avg7/avg30-aggregat (låg redan i PriceObservation.rawData; avg1→−1d, avg7→−7d, avg30→−30d). `getPriceHistoryBySource()` i `src/services/products.ts` grupperar observationer per dag/källa (cardmarket = Cardmarket+Pokémon TCG API+TCGdex API, tradera, butiker = övriga). `PriceChart` har källväxlare via `series`-prop (gamla `data`-prop kvar för dashboard/samling/landing). (4) **"Hos X butiker" = endast IN_STOCK**: alla ProductCard-anrop använder `inStockCount` istf `offerCount`. (5) Raw-copy på singelproduktsidor ("raw-kort Near Mint, inte graderade"). tsc + 71/71 unit + 20/20 sajttester gröna (fraktpristestet ersatt — frakt får vara null).
- [x] Produktsida-redesign 2026-06-12 (impeccable, Collectr-inspirerad): **Prishistoriken baseras enbart på Cardmarket** (Tradera-sålda går inte att hämta legitimt) — källväxlaren borttagen, `PriceChart` tog tillbaka enkel `data`-prop. Chart i Collectr-stil: blå monotone-kurva (#3b82f6), prickigt grid, endpoint-puls (halo + punkt på sista mätpunkten), vinklade datumticks (−40°), Y-ticks med adaptiva decimaler (spann <200 öre → 2 dec). Periodpiller 1M/3M/6M/1ÅR uppe till höger i chart-headern (länkar via `?period=`, default 3M). En ensam mätpunkt visas som centrerat nuläge (stort pris + "historiken byggs upp") istf flytande punkt. Produktsidan minimaliserad: breadcrumb › titel › meta-rad (set-länk · kategori · språk) ersätter badge-kluster; hero-grid `320px | 1fr` (bild | chart); 7d/30d-förändring beräknas från Cardmarket-serien (inte mixade PriceSnapshots som gav fel −71,7 %). Verifierad via Playwright-screenshots (desktop 1440 + mobil 390, multi-punkt + single-punkt + periodbyte). **PriceSnapshot = endast Cardmarket-marknadspris**: snapshots aggregerade tidigare ALLA källor (butiker+Tradera+CM) → skiftande källsammansättning gav fejkade ±2 296 747 %-rörelser på landningssidan. Fixat i runner.ts (aggregat filtreras på `CARDMARKET_SOURCE_NAMES`, exporterad från products.ts) + alla 59 794 snapshots ombyggda från CM-observationer (`scripts/rebuild-price-snapshots.ts`, 79 085 nya). Trendlistor (market.ts) har golv 10 kr + krav ≥2 snapshot-dagar — avg1 för bulk-kort svänger ±6000 % på en enda €0,02-försäljning (riktig data, meningslös som "rörelse"). PriceChange fick `whitespace-nowrap`. tsc + 71/71 unit + 20/20 sajttester gröna.

- [x] Tradera-API + CM-adapter + sealed-historik 2026-06-12: (1) **TraderaAdapter omskriven till officiellt SOAP-API** (api.tradera.com/v3/searchservice.asmx, AppId/AppKey i .env som `TRADERA_APP_ID`/`TRADERA_APP_KEY`, kvot 100 Search-anrop/dygn — `TRADERA_MAX_SEARCH_CALLS` default 30/körning × 3 körningar = 90). Endast fast-pris-listningar utan bud, EN-språkvakt via TermAttributeValues, exakta `/item/`-URL:er. Gamla skrapade Tradera-offers/observationer/restocks nollställda (`scripts/reset-tradera-offers.ts`) — 241 rena API-matchade offers efter svep. (2) **classifyForm utbyggd** (`src/scrapers/matching.ts`): lot-former multipack/case/combo hård-avvisas i matchProduct (stänger hålet där singlar med form=null gick förbi formvakten); mini tin före display, blister före N-pack, kvantitetsregler (ledande tal 2–20, "Nx", "N st/pkt", tal+formord efter interpunktion). (3) **Billigaste-annonsen-vinner** per `productId:retailerId:condition:language` i runner.ts + run-scrapers.ts (observationer sparas alltid). (4) **CardmarketPriceGuideAdapter** (`src/scrapers/adapters/cardmarket-adapter.ts`, källa "Cardmarket" typ API i runner) importerar CM-prisguiden automatiskt var 8:e h, idempotent per `guideCreatedAt` (en utgåva/dygn) — sealed-historiken ackumuleras utan manuella körningar. fs/path lazy-importeras med `webpackIgnore` (Edge-bundle via instrumentation). (5) **Arkiv-backfill**: enda kända Wayback-utgåvan av prisguiden (20241230185748) gav 277 riktiga historikpunkter daterade 2024-12-30 (`scripts/backfill-cm-priceguide-archive.ts`). (6) **MAX-period + smart fallback** på produktsidan: periodpiller 1M/3M/6M/1ÅR/MAX; har default-perioden <2 punkter men full historik ≥2 visas MAX automatiskt → äldre sealed får riktig kurva direkt; PriceChart visar årtal i ticks när serien spänner flera år. Nya produkter (t.ex. AH-bundle) visar ärligt nuläge tills kurvan byggts upp av dagliga CM-utgåvor. (7) **5 trasiga pokemoncenter.com-bilder ersatta** (Akamai blockerar hotlinking → text/html) med verifierade butiksbilder (`scripts/fix-broken-product-images.ts`). URL-byggare flyttade till `src/lib/marketplace-urls.ts` (scripts/ re-exportar). tsc + 71/71 tester gröna.
- [x] Daglig Tradera-fullsvep 2026-06-13: **6 separata API-kvoter = 600 anrop/dygn** (varje SOAP-metod har EGEN 100-anrop/24h-kvot; "Calls last minute" är Unlimited). Ny kärnmodul `src/jobs/tradera-sweep.ts` (`runTraderaSweep()`) delas av CLI-wrappern `scripts/tradera-full-sweep.ts` (laddar .env manuellt, ingen dotenv-dep) + jobb-workern. **Fas 1** (≤500 anrop): 5 sökmetoder (SearchService.Search/SearchAdvanced + PublicService.GetSearchResult/GetSearchResultAdvanced/GetSearchResultAdvancedXml) sökord "pokemon" × 4 kategorier (1001337 löskort, 1001340 boxar, 1001339 paket, 1001341 övrigt sealed) × prisband (Adv-metoder delar 0-30/30-80/80-200/200-500/500-2000/2000+kr för djupare täckning). **Fas 2** (≤100 anrop): GetSellerItems på topp-25 säljare från fas 1. **Fas 3** (0 anrop): offers ej sedda på `EXPIRY_DAYS` (7) nollställs → sök-URL + UNKNOWN (utgången annons). **Prislogik** per `productId:retailerId:condition:language`: billigaste matchande annonsen vinner; befintligt billigare pris behålls (bara lastSeenAt uppdateras); nytt/billigare → upsert med direkt `/item/`-länk. Matchar via `matchProduct` + `isPlausibleListingPrice` + singel↔sealed-vakt (categoryId 1001337 = singel). **Körschema**: EN gång/dygn — BullMQ repeat `0 4 * * *` (`tradera-sweep`-jobb i scheduler.ts + worker.ts), och i no-Redis-fallbacken körs svepet max en gång/kalenderdygn i 8h-ticken (24h-kvoten tål inte scrape-all:s 3×/dygn). SOAP-quirks: PublicService kräver AuthenticationHeader+ConfigurationHeader i SOAP-headern; GetSearchResultAdvanced använder gemener `<query>`; GetSearchResultAdvancedXml kräver queryXml inkapslad i `<Query>`-rot; PublicService använder `<ItemLink>` + nästlad `<Seller><Id>` (SearchService `<ItemUrl>` + `<SellerId>`). Täckning ~3 100+ direktlänkade annonser efter svep. tsc + 71/71 gröna.
- [x] Exakta CM-länkar + lot-vakt 2026-06-12 (kväll): (1) **Exakta Cardmarket-länkar för sealed**: `https://www.cardmarket.com/en/Pokemon/Products?idProduct={id}&language=1` — redirecten till produktsidan BEVARAR extra query-params, så language=1 förfiltrerar annonserna till engelska (verifierat via Playwright). Slug-URL:er går INTE att konstruera (ETB:er har slumpsuffix). `cardmarketProductUrl()` i marketplace-urls, används av CM-adaptern + `scripts/fix-cm-sealed-urls.ts` (328 offers omskrivna). (2) **Rimlighetsvakt mot lots** (`isPlausibleListingPrice` i matching.ts): Tradera-pris vs CM-marknadspris — sealed orimligt > 2,5×; singlar/graded orimligt först > 4× OCH > 400 kr över CM (svenska säljare prissätter billiga singlar legitimt långt över CM-trend). Vakt i runner.ts + run-scrapers.ts (skippar offer OCH observation — lot-pris är inte styckpris). `scripts/clean-implausible-tradera.ts` (dry-run/APPLY=1) sanerade 13 lots, bl.a. AH-bundeln (4 st för 4 200 kr; Traderas sid-JSON säger quantity:1 även för lots — endast prisration avslöjar). (3) **Tradera sålda-data**: SearchAdvanced(ItemStatus=Ended) + GetItem(GotWinner) finns i API:t men 100-anrop/dygn-kvoten gör systematisk såld-historik ogörlig — prishistorik förblir Cardmarket-only. (4) **CM:s egen prishistorik EJ åtkomlig legitimt**: publika prisguiden har avg1/avg7/avg30=null för sealed (endast trend); sajtens graf får ej skrapas. Historik byggs via dagliga guide-utgåvor + Wayback-punkten 2024-12-30. (5) traderaSearchUrl prefixar inte längre dubbelt "Pokemon" (40 URL:er fixade i DB). tsc + 71/71 gröna.

- [x] Designsystem v3 (neutral charcoal + teal) 2026-06-13: Användaren ville bort från den blåa paletten och ha ett mer proffsigt/seriöst uttryck (referens: en likartad samlartjänst — får ALDRIG nämnas i kod/copy). **Token-driven omfärgning** i `tailwind.config.ts` + `src/styles/globals.css` rippade hela sajten på en gång (alla komponenter använder token): navy `#0c1222`-familjen → neutral kol `#0a0a0c/#141417/#1d1d21/#2a2a30`; `ink` → neutrala zinc-toner (`#fafafa/#a1a1aa/#8a8a93`, kontroll: faint höjd till 5,3:1); `holo.cyan` (brand-accent, länkar, aktiva states, primärknapp, chart-linje) `#3b82f6` → turkos `#2dd4bf`; `brand` → teal; `rise` → tydligt grönt `#22c55e` (skilt från teal-accenten); `fall` → `#f43f5e`; shadows/glow + `holo-gradient` (nu mono-teal) + `surface-gradient` neutrala; `foil-line` mono-teal. Stray-färger fixade: `badge.tsx` holo-variant, `price-chart.tsx` (GRID/TICK/tooltip/endpoint neutrala, linjen redan turkos), `layout.tsx` themeColor, enda `text-slate-300`. **Aktiva nav-states blev teal** (ny client-komponent `src/components/layout/header-nav.tsx` ger marketing-headern aktivmarkering via usePathname; `app-shell.tsx` aktiv = `bg-holo-cyan/10 text-holo-cyan`). **Produktsidans chart** matchar nu önskad spec: titel "Ograderad prishistorik" för SINGLE_CARD, periodpiller aktiv = `bg-holo-cyan/15 text-holo-cyan`, turkos linje + nedtonad area-fill + tooltip (datum + pris + teal-prick + vertikal guide). **Utforska: butiksfilter** — `searchProducts` tar `retailerId` (kombineras med lager i ett `offers.some`-villkor), explore-sidan hämtar aktiva retailers och visar "Butik"-select. Verifierat: tsc rent, 71/71 unit gröna, Playwright-screenshots (landning/utforska/marknad/produkt/chart med hover) i nya paletten.

- [x] Endast direkta produktlänkar 2026-06-13: Användaren vill INTE se "Sök på Cardmarket/Tradera"-knappar — varje visad offer ska vara en **direkt produktlänk**, annars ska den inte visas. Ny delad vakt `isDirectOfferUrl(url)` i `src/lib/marketplace-urls.ts` (falskt vid `/search`, `searchstring=`, `?q=`/`&q=`). (1) **Produktsidan**: offers-API:t (`/api/products/[slug]/offers`) + sidan filtrerar offers till direkta länkar innan rendering OCH innan prisstatistik (lägsta/högsta/snitt + JSON-LD) räknas → panelen visar aldrig ett pris utan köpbar rad. `searchOffers`-knappblocket borttaget ur `live-product-pricing.tsx`. (2) **Butiksfilter** (`searchProducts.retailerId`): väljer man en butik visas bara produkter med en offer som är `IN_STOCK` + `price != null` + ej sök-URL (`NOT url contains 'search'`) → utgångna/osäkra Tradera-annonser (vi kan inte verifiera utan API-kvot) dyker inte upp. (3) **CM-backfill** `scripts/fix-cm-search-offers.ts` (dry-run / `APPLY=1`): konverterade 296 CM-sök-offers → direkt `prices.pokemontcg.io/cardmarket/{tcgExternalId}`. 40 produkter saknar tcgExternalId → kan inte få direkt CM-länk, döljs (avsiktligt). (4) **Tradera-täckning**: ~17 445 offers är fortfarande sök-länkar (kvot-begränsat) och döljs tills `runTraderaSweep()` (kräver mer API-kvot) hittar riktiga `/item/`-listningar och uppdaterar dem in-place. tsc + 71/71 gröna.

- [x] Saknade kort + prisförvirring 2026-06-13: (1) **PAGINERINGSBUGG fixad** (`fetchTcgCardsForSet` i `src/scrapers/adapters/pokemontcg-adapter.ts`): `orderBy=number` gav INSTABIL paginering hos pokemontcg.io (string-sortering → kort hamnade mellan sidorna och tappades). Set med >250 kort blev ofullständiga — t.ex. me2pt5 (Ascended Heroes) hade 250/295 kort, saknade #281 "Team Rocket's Mewtwo ex" (Special Illustration Rare). Fix: ta bort orderBy, dedupa på id, sortera på nummer i minnet. Tidigare CLAUDE-not ("me2pt5 har 250 unika kort i API:t") var FEL — det finns 295. (2) **8 trunkerade set om-importerade** (sv1/sv2/sv4/sv8/me2pt5/sm12/sm11/smp), +134 kort. (3) **`backfill-marketplace-offers.ts` fixad**: skapade CM-offers med SÖK-URL (döljs nu) → använder nu `cardmarketExactUrl(tcgExternalId)` direkt för singlar med TCG-id. Körd → 134 nya direkta CM-offers. (4) **Priceless direktlänkar visas**: `live-product-pricing.tsx` visar nu ALLA offers med direkt länk (inte bara prissatta) — pris "–" när marknadsdata saknas (t.ex. helt nya secret rares som me2pt5-281 som saknar CM/TCGplayer-pris). (5) **Prisförvirring förklarad (ej bugg)**: Suicune "H25/147" (ecard2-H25, HOLO) ≠ "AQ 37" (ecard2-37, non-holo). Vårt 623 kr = €54,2 = pokemontcg.io:s riktiga CM-trend för H25 (verifierat live). OBS semantik: "Lägsta pris just nu" för singlar = CM **trend**-pris (inte billigaste annonsen) — medvetet val, men etiketten kan vilseleda. (6) **Dubblettnummer-bugg + KATALOG KOMPLETT**: kortnummer är INTE unikt i ett set (Celebrations Classic Collection har 4 kort med nr 15; Black Bolt har två nr 60) → den gamla `@@unique([setId, number, language])` kollapsade varianter. Migration `card_key_tcgexternalid` gör `tcgExternalId` unikt (identitetsnyckel) och nummer+set till vanligt index; importen upsertar på tcgExternalId och ger nya produkter kollisionssäker slug (id-suffix vid krock). Full audit (`scripts/audit-set-completeness.mjs`): **0 ofullständiga set, 0 saknade kort** mot pokemontcg.io. tsc + 71/71 gröna.

- [x] Singel-erbjudande = lägsta pris (ej trend) 2026-06-13: Användaren ville att "Lägsta pris just nu" ska visa Cardmarkets faktiska lägsta annonspris ("From"/lowPrice), inte trend-priset. **Prishistoriken/grafen är OFÖRÄNDRAD** (PriceObservation/PriceSnapshot = trend — det är den enda historik vi har; lowPrice-historik finns inte). Endast `Offer.price` på singlar ändrad. Implementation: (1) `cardMarketLowOre()` i pokemontcg-adapter (lowPrice×1150, fallback trend); adaptern sätter nu `offerPrice` (=low) separat från `price` (=trend). (2) Nytt fält `offerPrice?` på RawProductData/NormalizedProduct. (3) runner.ts använder `offerPrice ?? price` för Offer + restock + prisalert, men `price` (trend) för observation + snapshot/graf → grafen bryts inte. (4) `backfill-marketplace-offers.ts` läser lowPrice ur observationens rawData (`singleOfferPrice()`). (5) `scripts/set-cm-offer-lowprice.ts` (APPLY=1) satte 18 701 befintliga CM-singel-offers till lowPrice (DISTINCT ON senaste obs med rawData.cardmarket.prices.lowPrice). 1 345 saknar lowPrice → behåller trend (graceful). Ex: Suicune H25 623 kr (trend) → 228,85 kr (€19,9 From). OBS visuell följd: grafen (trend ~575) och rubriken (lägsta ~229) skiljer sig — olika mått, tydligt etiketterade. tsc + 71/71 gröna.

- [x] Singel-CM pris+länk-ombyggnad 2026-06-13: Användaren hittade fel priser OCH fel länkar (Zekrom Celebrations visade 236 kr + länk till FEL kort). **Rotorsak**: CM-katalogen (`products_singles_6.json`) saknar kortnummer → namn-matchning förväxlade varianter (~80 "Zekrom" med identiska attacknamn i olika expansions; vår fick idProduct 576755 = "Zekrom [Field Crush]" = fel kort). pokemontcg.io saknar HELT CM-mappning för reprints (Celebrations Classic Collection). **Lösning** (`scripts/rebuild-cm-singles.ts`): bygg om ALLA singel-CM-offers per kort (nyckel=tcgExternalId, ingen variant-förväxling): PRIS = pokemontcg.io `cardmarket.prices.lowPrice` ("From") ur rawData (DB, ingen rate-limit-radering); LÄNK = cachad exakt slug (.cache/cm-resolved-urls.json, ?language=1) annars `prices.pokemontcg.io/cardmarket/{id}` (kort-specifik 302). Saknas lowPrice → INGEN CM-mappning → offer RADERAS (hellre tomt än fel). Resultat: 19 017 offers korrekta, 1 313 falska/omappbara raderade. Verifierat slumpsample: pris matchar CM + länk→rätt kort. **Lärdom**: pokemontcg.io per-kort-data (tcgExternalId) är enda pålitliga CM-källan per kort; CM-katalogens namn-matchning är OPÅLITLIG (saknar nummer). Länkar kan uppgraderas redirect→slug via `resolve-cm-urls.ts`.

- [x] Live växelkurs + engelska CM-länkar överallt 2026-06-13 (kväll): Användaren undrade var priserna kom ifrån (svar: CM lowPrice "From" × kurs — 321,89 kr = 27,99 € × 11,50, alltså korrekt) och krävde att ALLA Cardmarket-länkar ska vara den **rätta kort-länken med engelskt språkfilter** (`?language=1`). (1) **Live EUR→SEK/USD-kurs** (`src/lib/exchange-rate.ts`, ny): Frankfurter (`api.frankfurter.dev/v1/latest?base=EUR&symbols=SEK,USD`, gratis/ingen nyckel), dygnscache `.cache/exchange-rate.json`, fallback-kedja `EUR_SEK`-env-pin → cache → konstanter 1150/1050. `getRatesOre()` (async, hämtar+cachar) + `getCachedRatesOre()` (synkron). fs/path lazy-importeras (webpackIgnore) för Edge-grafen. Ersatte ~10 hårdkodade `1150` i adaptrar + ingest-scripts. Verifierat: 1 € = **10,93 kr** (gamla 11,50 var ~5 % för högt → singelpriser sjönk ~5 %, t.ex. Serperior ex 164/86 321,89 → 305,93 kr). Grafhistoriken lämnas i lagrad kurs (FX-drift liten). (2) **Engelska länkar på ALLA visade singlar**: `prices.pokemontcg.io/cardmarket/{id}`-redirecten STRIPPAR `?language=1` (verifierat: identisk Location med/utan) → enda vägen är att lösa redirecten till slug och sätta language=1 på den. `resolve-cm-urls.ts` ombyggd till **streaming worker-pool** (CONCURRENCY-workers drar från delad cursor, ingen batch-barriär — en 9 s-tarpit blockerar inte de andra; default conc 12, 8 s request-timeout via AbortSignal, 429/5xx/timeout-backoff). Batch-barriär-versionen gav ~24/min; poolen ~590/min. Körd: 6 622 redirect→slug, 1 omappbar, 40 utan tcg-id. (3) `rebuild-cm-singles.ts`: skriver ALDRIG en bar redirect som visad länk (slug ur cache, annars bara prisuppdatering / skip) + live kurs. Körd APPLY=1: **18 785 offers fick exakt engelsk slug-länk + live-pris**. (4) **Durabilitetsfix i `runner.ts`**: PokemonTcgAdapter emittar `card.cardmarket.url` (= redirecten) → 8h-jobbet skrev annars över våra slug-länkar var 8:e timme. Runnern bevarar nu en redan löst engelsk slug (`isEnglishCardmarketUrl`) framför en inkommande redirect. (5) `isDirectOfferUrl()` döljer nu bara-redirecten (`isCardmarketRedirect`) → en oengelsk länk visas ALDRIG; olösta döljs tills resolver uppgraderat dem. Backfill/fix-scripts föredrar slug-cachen. **Verifierat: 0 visade CM-singel-länkar saknar language=1** (14 olösta dolda), Serperior ex 164/86 = 305,93 kr → exakt engelsk BLK164-slug. tsc + 71/71 gröna.

- [x] Singelpris = Cardmarket-TREND (inte lowPrice) 2026-06-13 (natt): Användaren hittade absurt låga priser — Celesteela-GX SM67 visade 10,93 kr (= €1 × 10,93) medan CM "From" = €4. **Rotorsak**: pokemontcg.io `lowPrice` = CM:s all-språk/all-skick-GOLV (en utländsk/skadad kopia), INTE det engelska "From" användaren ser. För dyra kort sammanfaller de (Serperior lowPrice €27,99 ≈ From €28), för billiga bulk-kort ligger golvet långt under (€1 vs €4). **Inget flöde ger engelska "From"** (pokemontcg.io & CM-prisguiden ger bara all-språk; CM-katalogen saknar nummer/slug → idProduct ej mappbart; CM-sidan blockerar scraping 403). **Beslut (användaren valde):** visa **Cardmarket trendPrice** (marknadspris) × live-kurs för ALLA singlar — matchar grafen, inga absurda golv. Ändringar: `rebuild-cm-singles.ts` SQL `lowPrice`→`trendPrice`; `pokemontcg-adapter` `offerPrice = priceOre` (trend, `cardMarketLowOre` borttagen); `backfill-marketplace-offers.singleOfferPrice` = `observation.price` (trend); `set-cm-offer-lowprice.ts` markerad FÖRÅLDRAD (kör ej). **Etikett**: produktsidans rubrik för SINGLE_CARD = "Marknadspris (Cardmarket)" (sealed behåller "Lägsta pris just nu") via `priceLabel`-prop i `LivePricePanel`. **Färsk-import**: `import-tcg-data.ts` kördes om (173 set, 20 090 kort, 19 424 färska prisobservationer) → rebuild APPLY=1 satte 18 778 singel-offers till trend×kurs. Verifierat: Celesteela SM67 10,93 → **48,86 kr** (€4,47 trend), Serperior 164 → 360,58 kr; 0 visade CM-singel-länkar utan language=1 (17 dolda). Två öppna ärenden sparade i minnet (lowPrice-vs-From-gapet + ~1300 reprints utan CM-mappning). tsc + 71/71 gröna. OBS: trend ligger ofta NÅGOT över "From" — medvetet val.

- [x] Sealed-priser/länkar + chart-interaktion 2026-06-13 (sent): (1) **Sealed (boxar/paket/ETB/bundles)**: CM-länkar var redan engelska (idProduct?language=1). Problemet var fel-billiga butiks-/Tradera-offers som blev vilseledande "lägsta pris" (t.ex. "Paldea Adventure Chest" 749 kr på en Booster Box). **`isDirectOfferUrl` läckte** Spelexpertens `sokstr=`/`funk=sok`-söklänkar + Alphaspels `query=` → utökad vakt döljer dem nu. Rensade fel-matchade sealed-offers med `scripts/clean-sealed-offers.ts` (**median-konsensus**: outlier < 40 % av medianen bland ≥3 direkt-offers — robust mot uppblåst CM-ankare; raderade 11). Verifierat: Paldea Evolved Booster Box 749 → **2099 kr** (Dragon's Lair), Mega Evolution box 2149 kr, Journey Together ETB 779 kr. **Öppet ärende** (sparat i minnet): vissa sealed CM-trendpriser är uppblåsta pga felmappad idProduct (t.ex. Brilliant Stars box €795) — headline-lägsta är ändå rätt (butik vinner), men CM-raden i tabellen kan vara fel; kräver bättre sealed→idProduct-mappning. (2) **Prishistorik-chart omdesignad** (`price-chart.tsx`, impeccable): scrub/hover ger vertikal markör (mjukt tonad turkos linje) + tooltip vid vald punkt; **linjen FÖRE punkten är skarp, EFTER tonar den ut mjukt till spöklik** via en horisontell stroke-gradient vars brytpunkt = `activeTooltipIndex/lastIndex` (client-state, `useId`-namnsatta gradienter för flera charts/sida); rundade stroke-ändar, fokus-prick vid vald punkt, dämpad endpoint-puls i idle. Reduced-motion respekteras (`.price-fade-stop`-transition nollas av global regel). Verifierat via Playwright-screenshots (idle + hover 40 %/72 %). tsc + 71/71 gröna.

- [x] App-funktioner + FX-slutstädning 2026-06-14: (1) **Live-FX-migration slutförd** — `backfill-cm-priceguide-archive.ts`, `backfill-singles-history.ts` använder nu `getRatesOre()` (var hårdkodade 11.5); `mega-fix.ts` raderad (fabricerade priser, bröt mot regeln); kvarvarande `11.50`/`1150` är endast `prisma/seed.ts` (medveten ögonblicksbild) + `EUR_FALLBACK_ORE`. (2) **"Marknadspris" → "Marknadstrend (Cardmarket)"** på produktsidan (rubrik + chart-underrubrik). (3) **Skanna → värde → live-portfölj**: skannade kandidater + bekräftelse visar marknadsvärde (`getCardValues`); `computeCollectionValue` värderar nu LIVE (CM-trend × kurs, `valueCollectionItems`) med snapshot-fallback; samlingstabellen visar live per-kort + total. (4) **AI-gradering** (`/gradera`, `src/services/grading/*`): fram-/baksidesbild → PSA-liknande 1–10 + delpoäng (centrering/hörn/kanter/yta) via Claude vision (forcerat `report_grade`-verktyg), `GradingJob`-modell (migration `grading_job`), tiered FREE Haiku 5/dygn / PREMIUM Sonnet, mock i dev, ärlig "ej officiell PSA"-disclaimer, nav-länk + länk från `/skanna`. (5) **PWA**: `public/sw.js` + `pwa-register.tsx` (prod) → installerbar. `@anthropic-ai/sdk` tillagd. (6) **Build-fix + ikoner**: dynamiska `src/app/icon.tsx` (`next/og`/@vercel/og) raderad — den kraschade `next build` ("Invalid URL" vid static export); favikon serveras nu statiskt via metadata + `public/icon.svg`. Manifestet pekade på SAKNADE `icon-192/512.png` → genererade dem (+ `apple-icon.png`) från en ombrandad teal `icon.svg` via `scripts/generate-icons.mjs` (`npm run icons:gen`, `sharp` devDep); manifest theme/background `#0B0E14` → `#0a0a0c`. **`npm run build` är nu grön** (alla 35 sidor inkl. `/gradera`). Verifierat: tsc rent, 81/81 unit gröna, build grön, smoke (ikoner/manifest/sw 200, `/gradera` 307, grading-API 401, produktsida visar "Marknadstrend (Cardmarket)").

- [x] RapidAPI (CardMarket API TCG) = engelska NM-lägsta "From" för ALLA produkter 2026-06-14: Användaren skaffade RapidAPI-nyckeln (`CARDMARKET_RAPIDAPI_*`) som ÄNTLIGEN ger det engelska NM-From-priset (löser de två öppna ärendena om singelpris-källa). **Pro-plan = 3000 anrop/dygn, 300/min** (verifierat i `X-RateLimit-*`-headers). Fält: `prices.cardmarket.lowest_near_mint` (DECIMAL EUR; bas=engelska, `_DE/_FR/_ES/_IT`=språk-överstyrningar), sealed = `prices.cardmarket.lowest`. (1) **Singlar** (`scripts/rapidapi-fill-singles.ts`): set-paginering `/pokemon/episodes/{id}/cards?page=N` (20/anrop, fulla priser+`cardmarket_id`), matcha `tcgid`=`tcgExternalId` → **19 683/20 359 matchade** (98%), 17 852 uppdaterade + 963 nya offers, 210 outlier-skyddade, 868 utan CM-pris. Priset = `lowest_near_mint × live-kurs`; outlier-vakt `>4× 30d-snitt → snitt` (t.ex. Ampharos €350→€66,79=730 kr). (2) **Near Mint-länkar**: alla singel-CM-länkar fick `&minCondition=2` (NM och bättre) ovanpå `?language=1` via `withNearMint()` (`src/lib/marketplace-urls.ts`) — bulk-SQL `scripts/cardmarket-add-nm-filter.ts` (18 897 länkar) + fyllningen. (3) **Sealed** (`scripts/rapidapi-fill-sealed.ts`): hämtar hela `/pokemon/products`-katalogen (~1900) i ~97 anrop (disk-cache `.cache/rapidapi-sealed.json`), matchar set+form (`classifyForm`)+namnlikhet (`scoreSimilarity`) → **283/357**, vakter: boosterbox kräver "booster" i API-namn (annars matchar "Tech Sticker Display" för box-lösa set som Prismatic/Paldean Fates), outlier ×2.5 vs 30d-snitt, butik-cross-check ×2.5 (skip om CM ≫ riktigt SE-butikspris). Sealed = `language=1` UTAN minCondition (inget skick). (4) **Durabilitet i `runner.ts`**: trend-källan (Pokémon TCG API/TCGdex) skriver inte längre över ett satt singel-offer-pris (`let offerPrice` + seed-only-vakt) → 8h-jobbet återställer inte From-priset; grafen/observationen är fortf. trend. (5) Rubrik singlar: "Marknadstrend" → **"Lägsta pris · NM engelska (Cardmarket)"**. Verktyg: `scripts/rapidapi-cm-compare.ts` (jämför enskilda kort vs CM). tsc + 81/81 unit gröna. **Lärdom**: tcggo:s `tcgid` = pokemontcg.io-id (vår `tcgExternalId`) även för z-prefix-set (Black Bolt); `?tcgid={id}` ger exakt 1-träffsuppslag.

- [x] EXAKT From + auto-refresh + restock 2026-06-14 (kväll): Användaren: "a lot of cards are wrong prices, before was right" (ex: Ampharos visade 730 kr men CM "From" = €350). **Rotorsak**: outlier-vakten (×4) bytte CM:s feldyra "From" mot 30d-snittet → matchade inte CM längre. Användaren VALDE (AskUserQuestion) "Exakt From — matcha CM 1:1" trots att enstaka annonser är feldyra/graderade (Ampharos enda NM-EN-annons = en "psa 8" på €350). (1) **Outlier-vakt BORTTAGEN** (`OUTLIER_MULT` default 0 i båda fill-scripten + `cardmarket-refresh.ts`): singel-pris = `lowest_near_mint` exakt, sealed = `lowest` exakt; fallback `30d_average` bara när lägsta saknas. Ampharos → €350 = 3826 kr (matchar CM). (2) **AUTOMATISK refresh** (användaren ville bort från manuell): ny modul `src/jobs/cardmarket-refresh.ts` (`runCardmarketRefresh()`, exakt From, inget disk-cache) körs EN gång/dygn — BullMQ `cardmarket-refresh` `0 5 * * *` (`scheduler.ts`+`worker.ts`) + once-per-day-vakt (`lastCmRefreshDay`) i BÅDE worker-fallbacken OCH `instrumentation.ts`-ticken (= körs även när bara dev-servern är igång). Tradera (dagligt svep 04:00 + 8h-adapter) och butiks-/CM-priceguide-adaptrar auto-körs redan var 8:e h via `runScheduledScrapesOnce`. (3) **Restock-alerts från svenska butiker**: redan implementerat (`checkRestockAlerts` i runner vid lagerövergång→IN_STOCK; 4 aktiva adaptrar Spelexperten/Webhallen/Dragon's Lair/Alphaspel kör var 8:e h). **Buggfix**: alert-länk `/produkt/` → `/produkter/` (404) i `src/services/alerts.ts` (pris+restock). **Spel & Sånt + Spelbutiken saknar adapter** (inaktiva; robots.txt för Spel & Sånt ej verifierad) → kräver nya adaptrar för att ge restock (ej byggda än). tsc grön.

- [x] Komplett sealed-katalog + gömda kategorier 2026-06-14 (natt): (1) **TINS fanns inte alls** (0 produkter → tom kategori) + sealed-katalogen var gles. Ny `scripts/import-sealed-from-cardmarket.ts` skapar sealed-produkter ur hela CM-katalogen (`/pokemon/products`, ~1900) som vi saknar: `classifyForm`→kategori (display→BOOSTER_BOX, booster→BOOSTER_PACK, etb→ETB, collection→COLLECTION_BOX, tin→TIN, blister→BLISTER, bundle→BUNDLE; case/multipack/accessory/combo/deck skippas), pris = CM `lowest` (exakt) × kurs, **lagerstatus = `available_items`** (>0 IN_STOCK, 0 OUT_OF_STOCK), länk `idProduct&language=1`, set länkas via episode-namn (annars `setId=null` — OK). Dedup: cardmarket_id ur befintliga offer-URL:er (`idProduct=`) + (normTitle|kategori). **+1201 nya** (375 tins, 358 blisters, 250 collection, 115 pack, 54 ETB, 29 bundle, 21 box) → sealed ~357→~1558. (2) **Auto-refresh uppgraderad** (`cardmarket-refresh.ts`): sealed matchas nu PRIMÄRT på `cardmarket_id` (idProduct ur offer-URL) → täcker även set-lösa produkter (tins) som inte kan fuzzy-matchas på set; fuzzy (set+form+namn) är fallback för produkter utan CM-offer; uppdaterar pris OCH lagerstatus (available_items). (3) **Gömda kategorier**: användaren bad ta bort Tillbehör (ACCESSORY), Gradat kort (GRADED_CARD), Övrigt (OTHER) "för nu". `HIDDEN_CATEGORIES` i `src/services/products.ts` (exporterad) → `searchProducts` utesluter dem ur katalogen (även vid direkt `?kategori=`-anrop), `produkter/page.tsx` filtrerar bort dem ur kategori-dropdownen. Produkterna raderas INTE (8 ACCESSORY finns kvar, kan återinföras). tsc + 81/81 gröna; tin-produktsida (set-lös) + katalogfilter verifierade 200.

- [x] Falsk "i lager" + språkfilter 2026-06-14 (sent): (1) **Falsk "i lager" + uppblåst pris**: importen föll tillbaka på CM `30d_average` när `lowest` saknades → vintage-snitt visades som "Lägsta pris just nu" (t.ex. Lucario Tin 10 086 kr = €922,86 30d-snitt) + `available_items=1` gav falsk "I lager". **Fix**: sealed-pris = ENDAST `lowest` (ingen 30d-fallback); lager = IN_STOCK bara när `lowest` finns (annars UNKNOWN/`available_items===0`→OUT). Ändrat i `import-sealed-from-cardmarket.ts` + `cardmarket-refresh.ts` (sätter ALLTID price, även null, så gammalt uppblåst pris nollas). Backfill `scripts/fix-sealed-stock-price.ts` (cache-baserad, ingen kvot): **294 priser nollade, 175 lagerstatusar rättade** av 1523 sealed-CM-offers. Lucario Tin → pris "–", UNKNOWN, ingen "I lager"-badge (verifierat). (2) **Språkfilter** (`produkter/page.tsx`): `BROWSE_LANGUAGES = ["EN","JP"]` → katalogens språk-dropdown visar bara Alla språk/Engelska/Japanska (Svenska/Tyska/Franska/Övriga borttagna; verifierat). (3) **Sparat i minne**: Spel & Sånt + Spelbutiken-adaptrar att bygga senare ([[project-pending-store-adapters]]). tsc + 81/81 gröna.

- [x] Sealed "Ur lager" + 30d-pris-modell 2026-06-14 (natt, ERSÄTTER blank-pris ovan): Användaren ville INTE blanka priset för outliers/slut-i-lager — visa istället 30d-snittet med en "Ur lager"-badge. **Modell (alla sealed, ej bara tins)**: `lowest`/From finns → **IN_STOCK** + From-pris (grön "I lager"); ingen `lowest` men `30d_average` finns → **OUT_OF_STOCK** + 30d-snitt (amber "Ur lager"); ingendera → pris "–". Dagliga `cardmarket-refresh` flippar dynamiskt mellan dessa per `cardmarket_id` (slut→i lager byter 30d→From, och tvärtom). (1) **Badge** (`src/components/ui/badge.tsx`): OUT_OF_STOCK-etikett "Slut i lager"→**"Ur lager"**, variant `danger`(röd)→**`warning`(amber/holo-gold)**. `computeLowestPrice` (products.ts) + offers-API:t visar redan billigaste prissatta offer MED dess stockStatus (föredrar IN_STOCK) → modellen renderas automatiskt. (2) Logik ändrad i `import-sealed-from-cardmarket.ts`, `cardmarket-refresh.ts`, backfill `fix-sealed-stock-price.ts` (kör om: **293 sealed "Ur lager" + 30d-pris, 1210 i lager**). Lucario Tin → **10 086 kr + "Ur lager" (amber)**, verifierat på produktsidan. OBS: detta upphäver gårdagens "blanka priset när lowest saknas". tsc + 81/81 gröna.

- [x] Sealed utan offer → prissatta 2026-06-14 (natt 2): Användaren såg collection boxes med "–" (Tag Team Reshiram/Pikachu GX Premium, Rapid Strike Urshifu VMAX Premium). **Rotorsak**: 29 sealed-produkter saknade prissatt CM-offer — 8 helt utan offer, 20 med offer vars `cardmarket_id` finns i katalogen MEN utan pris (vintage-boxar Majestic Dawn/Crystal Guardians + "(6 Cards)"-skräp = genuint ingen CM-marknadsdata). Premium-collections fanns i katalogen MED pris men `cardmarket_id=null` → importen skapade produkt utan offer (skapar bara offer om cmid finns). **Fix** `scripts/fix-sealed-no-offers.ts`: matchar orphans mot HELA katalogen på form+namnlikhet (tröskel **0,85** — lägre gav katastrofträff "Shrouded Fable Booster Box"→"Unleashed Booster Box" 491 850 kr eftersom Shrouded Fable saknar riktig boosterbox), pris+lager enligt samma modell, länk = `idProduct` annars katalogens CM-redirect (`links.cardmarket`/tcggo, klarar `isDirectOfferUrl`). **5 prissatta** (3 collection boxes 765/2831/3060 kr + Incineroar/Torterra Special Collection + Ascended Heroes PC ETB). **24/1558 kvar "–"** = genuint utan CM-marknadsdata (ärligt). OBS: cmid-null-produkterna (tcggo-länk, setId null) auto-refreshas EJ av dagliga jobbet (varken cmid- eller set-match) — engångspris satt. tsc + 81/81 gröna.

- [x] Infinite scroll-katalog + göm prislösa 2026-06-14 (natt 3): (1) **Utforska visade bara 500** (`MAX_CANDIDATES`-cap + in-memory-sortering) + sid-väljare. Användaren ville EN scrollbar feed över alla ~21k, ingen sidväljare, optimerad. **Lösning**: denormaliserat `Product.lowestPriceOre` (öre, null=prislös) via `db push` (additiv, ingen migration-reset) + `@@index`. `recomputeProductPriceCache()` (`services/products.ts`, 1–2 s, två UPDATE) räknar om — körs efter scrape (`runScheduledScrapesOnce`) + daglig CM-refresh (`cardmarket-refresh.ts`). Ny `getExploreFeed(params, offset, limit)`: DB-sorterbara sorteringar (popular/price_asc/price_desc/most_watched) paginerar över HELA katalogen (`orderBy` + `skip/take`, prissortering via `lowestPriceOre`); beräknade (prisfall/trend/restock) körs över topp-500 (scroll stannar där). `buildProductWhere` extraherad + delad av `searchProducts` (landing/api) + feeden. Feed-API `/api/products/feed` (offset/limit). Klient `ExploreFeed` (`components/features/explore-feed.tsx`, "use client"): SSR första 24, `IntersectionObserver` (rootMargin 800px) hämtar 24 åt gången → ingen lagg, ingen 20k-DOM på en gång; `key={feedQuery}` remountar vid filterbyte. Sidan visar nu "**21 602 produkter hittades**" (var 500). PageLinks/pageHref borttagna. (2) **Göm prislösa** (= den begärda "göm 24 sealed tills de dyker upp igen"): `lowestPriceOre = null` → `buildProductWhere` filtrerar bort dem ur katalogen; när de får ett pris (Cardmarket/Tradera) sätter nästa recompute `lowestPriceOre` → de dyker upp AUTOMATISKT igen. 337 dolda (24 sealed + bulk-singlar utan pris). **€0,00-offers räknas inte som pris** (`computeLowestPrice` + recompute + offers-API: `price > 0`) → 21 €0-bulk-kort visas inte längre som "0 kr" först i prissortering. `scripts/recompute-price-cache.ts` för manuell omräkning. tsc + 81/81 gröna; verifierat: 24 SSR-kort, feed-API paginerar + prissorterar. OBS: `lowestPriceOre` lades via `db push` (ej migration-fil) — kör inte `migrate dev` utan att lösa drift.

- [x] Restock-alerts butiker — Wave 1 (Shopify + frekvent bevakning) 2026-06-14 (natt 4): Användaren ville restock-alerts för ~13 svenska Pokémon-butiker, optimerat för TIDIGA alerts + låg last (vald cadence ~30–60 min; bygg-ordning: återanvändbara plattformar först). **Plattformskartläggning** (curl-probe): Shopify (`/products.json` med `available`-lager, robots OK) = speltrollet.se, samlarhobby.se, goblinen.com; WooCommerce-API = INGEN (arcadedreams gav HTML-catch-all, ej äkta); Quickbutik = swepoke/shinycards; Starweb = coolcard; custom/JS = maxgaming/sweetnerds/spelochsant(Spel&Sånt)/playoteket; redan adapter = alphaspel/webhallen/spelexperten. **Wave 1 byggt**: återanvändbar `src/scrapers/adapters/shopify-adapter.ts` (abstrakt `ShopifyAdapter` + tunna subklasser Speltrollet/Samlarhobby/Goblinen; hämtar pokemon-kollektioner via `/collections.json` → `/collections/{h}/products.json`, lager=variant.available, politeFetch/robots). Registrerade i runner `SCRAPER_ADAPTERS`. `scripts/setup-restock-sources.ts` skapar källorna + flaggar `config.restockWatch=true` (3 Shopify + befintliga Webhallen/Spelexperten/Alphaspel = 6 källor). **Frekvent restock-watch** `src/jobs/restock-watch.ts` (`runRestockWatch()`): kör enbart restockWatch-källor + dispatch — schemalagt var `RESTOCK_WATCH_MINUTES` (default 45) i instrumentation (egen interval, oberoende av 8h-scrape) + BullMQ `restock-watch` + worker-fallback. Restock-detektering återanvänder runnerns RestockEvent + `checkRestockAlerts` (OUT→IN). CLI: `scripts/restock-watch-run.ts`. Verifierat: Speltrollet-adapter 363 produkter/0 fel, matchade 26 → 24 offers skapade. **Wave 2 (kvar)**: Quickbutik (swepoke, shinycards) + Starweb (coolcard) via sitemap+produktsida. **Wave 3 (kvar)**: custom/JS (maxgaming, sweetnerds, Spel & Sånt, playoteket, arcadedreams) — fragila HTML-scrapers. OBS: Shopify-butiker som DÖLJER slutsålt visar ev. inte OUT-läget (svagare restock-detektion); de flesta behåller `available:false`. tsc + 81/81 gröna.

- [x] Restock-alerts Wave 2 (Quickbutik) + recon 2026-06-14 (natt 5): "build the rest". **Quickbutik byggt** `src/scrapers/adapters/quickbutik-adapter.ts` (abstrakt + Swepoke/Shinycards): sitemap → /pokemon/{kat}-sidor (SEALED-only — hoppar singles/graded/tillbehör, snabbare+politare). **LÄRDOM: samma plattform, olika TEMA** — swepoke (`block px-6 mb-4`/`<h3>`/`text-accent`) vs shinycards (`product-price`/`Lägg i korgen`, titel ej i h3). Lösning: parsa Quickbutiks **tema-oberoende data-attribut** `<div data-pid data-s-price data-s-title>` (primärt) med gamla tema-parsern som fallback → BÅDA fungerar (swepoke 12 / shinycards 64 boosterboxar/sida, korrekt lager). Last sänkt (MAX_CATEGORIES 18, MAX_PAGES 4, delay 900 ms) — swepoke var 5:47 (för tungt för 45-min-loop). Registrerade i runner + setup-script (`restockWatch`). = **8 källor live**. **Recon på resten (sparad i [[project-pending-store-adapters]])**: coolcard (Starweb) = LÅGT värde (bara accessory-kategorier); sweetnerds = JS-SPA (ej HTML-skrapbar snällt); maxgaming = kategori-URL 404 på gissning; arcadedreams = WP, icke-standard HTML; spelochsant (Spel&Sånt) = 281-byte/blockerad; playoteket/Spelbutiken ej utredda. Slutsats: resterande är bespoke/fragila/delvis omöjliga — byggs en i taget MED verifiering (undvik bräckliga scrapers; datakvalitet > täckning). tsc grön.

- [x] Git/GitHub + sealed-bildfix 2026-06-15: (1) **Projektet versionshanteras nu** — privat repo `github.com/8anKy/Foilio` (gh CLI, konto 8anKy). `.gitignore` utökad: `.cache/`, `.scratch/`, `.claude/settings.local.json`, `*.tsbuildinfo`, `next-env.d.ts`. Verifierat att inga hemligheter trackas (RapidAPI/Tradera-nycklar finns ENDAST i `.env` = ignorerad; lokala dev-DB-lösenordet i CLAUDE.md är ofarligt i privat repo). DB-datan ligger kvar i lokal Postgres (EJ i git) — migreras vid hosting. (2) **Sealed-bilder fixade** (`scripts/fix-sealed-images.ts`, APPLY=1): 332 sealed-produkter visade fel bild — singel-kort-art från `images.pokemontcg.io` (229) eller EN set-bild återanvänd över alla former (box-bild på pack/ETB/blister, 89 Amazon). Re-pekade varje CM-matchad sealed (cardmarket_id ur offer-URL) till CM-katalogens per-produkt-bild på `images.tcggo.com` (samma källa vi prissätter från → bilden matchar pris+länk, rätt per form: pack→`-booster.png`, box→`-booster-box.png`). Resultat: tcggo 1184→1509, pokemontcg.io 229→5, form-krockar 120→3. Residual ~20 (vintage EX-boxar/CM-id-lösa) saknar tcggo-bild = ärligt orört. **Durabilitet**: `cardmarket-refresh.ts` self-healar nu sealed-bilden vid EXAKT cmid-match (`imageUrl=best.image`) → bilder hålls rätt dagligen. tsc grön, committat+pushat.
- [x] Batch-roadmap 2026-06-15 (pågående): användaren gav 5 önskemål — (1) restock Wave 3 (kvar), (2) sealed-bilder ✅, (3) live-kort-identifierare ✅, (4) always-on datainsamling/hosting (kvar — kräver beslut lokal-worker vs moln), (5) iOS/Android-app via Capacitor (kvar — kräver hosting först + Apple/Google-konton; iOS-bygge kräver Mac/cloud-build, användaren på Windows). Rekommenderad ordning: git ✅ → sealed-bilder ✅ → resten. Tasks spåras i task-systemet.

- [x] Live kort-identifierare 2026-06-15: Användaren ville en LIVE-skanner (skild från gradering): håll upp ett kort framför kameran UTAN att ta foto → kortet identifieras i realtid + visar aktuellt pris. (Gradering `/gradera` = fram/bak-FOTO → betyg, OFÖRÄNDRAD.) Byggt: (1) `ClaudeVisionOcrAdapter` (`src/services/scanner/claude-vision.ts`) — läser namn+nummer ur EN ruta via Claude Haiku, tvingat verktyg `report_card`; aktiveras med `OCR_PROVIDER=claude` + `ANTHROPIC_API_KEY` (`SCANNER_MODEL` default haiku; mock kvar i dev). (2) Stateless `identifyCard()` + `POST /api/scanner/identify` — skapar INGEN ScannerJob per ruta (billigt att polla), rate-limitad 120/min. (3) `ScanCandidate.slug` (produkt-djuplänk) via produkt-lookup i `matchCards`. (4) Omskriven `/skanna`-sida (client): getUserMedia (environment-kamera), nedskalad ruta (≤640px JPEG) var 1,5 s → identify → live-resultat (kort + pris + säkerhet), LÅSER vid 2 lika kort i rad, "Lägg till i samlingen" (via `/api/collection`) + "Visa produkt", uppladdnings-fallback för kameralösa enheter. (5) **Buggfix `rateLimit`** (`src/lib/rate-limit.ts`): kastade 500 när Redis var konfigurerad men nere (`enableOfflineQueue:false` → "Connection is closed") → try/catch faller nu tillbaka på in-memory. Drabbade ALLA rate-limitade routes utan Redis (latent bug min live-loop exponerade). Verifierat via Playwright + Chromium fake-kamera (login→kamera live→identify→resultatpanel med pris + knappar). tsc + 81/81 gröna.

- [x] Molnhosting-scaffolding (gratis 24/7) 2026-06-15: Användaren valde gratis 24/7 moln (så CM/Tradera uppdateras när datorn är AV). **Arkitektur**: Neon (gratis serverless Postgres — DB=181 MB ryms i 0,5 GB-gränsen) + **GitHub Actions** (schemalagda jobb, gratis 2000 min/mån privat repo) + Vercel (gratis Hobby-webb). Idag körs allt i `src/instrumentation.ts` = bara när `npm run dev` lever; molnet flyttar schemaläggningen till GH Actions. **Byggt (repo-sidan, klart)**: CLI-wrappers `scripts/cardmarket-refresh-run.ts` + `scripts/scrape-all-run.ts` (restock-watch-run + tradera-full-sweep fanns redan); 4 workflows i `.github/workflows/` — `cardmarket-refresh` (dagligen 05:00 UTC), `tradera-sweep` (dagligen 04:00), `restock-watch` (var 2:e h), `scrape-all` (dagligen 02:00, ej 8h pga Tradera-dygnskvot). Varje workflow: checkout→node 20→`npm ci --legacy-peer-deps`→`prisma generate`→tsx-script, env från repo-secrets, `workflow_dispatch` för manuell körning. `docs/HOSTING.md` = steg-för-steg. På Vercel sätts `SCRAPE_INTERVAL_MINUTES=0`+`RESTOCK_WATCH_MINUTES=0` (GH Actions schemalägger). **KVAR (kräver användarens konton)**: skapa Neon + Vercel, migrera data (`pg_dump local | psql neon`, el. jag kör om Neon-URL delas), lägg GH-secrets (DATABASE_URL, CARDMARKET_RAPIDAPI_*, TRADERA_*) + Vercel-env. Restock var 2:e h (ej 45 min) för att rymmas i gratis-minuterna — publikt repo = obegränsade Actions. tsc grön.

- [x] **PRODUKTION LIVE + skanner-omgörning + perf 2026-06-16**: (1) **Vercel-webben deployad, live på https://foilio.se** (Neon-DB funkar i drift). Deploy = `vercel --prod` CLI; **commit-författarens e-post MÅSTE vara `milostheboss123@gmail.com`** (Vercel-kontot) annars blockeras deployen (GitHub `8anKy` ≠ Vercel-teammedlem); Node **22.x** (24.x provisionerade ej); `NEXT_PUBLIC_APP_URL=https://foilio.se` + `OCR_PROVIDER`/`GRADING_PROVIDER=claude` satta i Vercel-env (skanner/gradering kör nu riktig Claude). Prebuilt-deploy från Windows = återvändsgränd (symlink-EPERM). Detaljer i minnet [[project-resume-point]]. (2) **Skanner: LIVE-loop → CAPTURE-baserad** (`src/app/(app)/skanna/page.tsx`, Collectr-stil): fullskärms-overlay m. kortram + slutare → TRYCK fångar EN ruta (canvas→JPEG, sparas ALDRIG i kamerarullen), träffar i en strip m. löpande total, "Granska träffar"-vy (per-kort skick/språk/antal + bulk-add), scan-details-sheet (Din bild vs Din träff + alternativ-val). 1,5 s-live-loopen BORTTAGEN (ingen konstant CPU/nätverk). z-skala: cookie 50 < overlay 60 < toast 70. Identifiering via samma `/api/scanner/identify` (precise=Sonnet per tryck). (3) **Perf**: recharts lazy även på landning/dashboard/samling (`PriceChartLazy`) → First Load JS −~100 kB/route (/ 207→105, dashboard 212→111, samling 216→115); kort-bilder redan lazy. tsc + `npm run build` gröna; skanner verifierad via Playwright fake-kamera (launch→capture→strip→review→details).

- [x] **Datakvalitet + perf (användarfeedback) 2026-06-16 (kväll)**: (1) **REGION-FIX = största perf-vinsten**: Vercel-funktionerna körde i `iad1` (US-East) men Neon ligger i `eu-central-1` (Frankfurt) → varje DB-query korsade Atlanten (~100 ms × många/sida = 1,5–2,8 s varm sida). `vercel.json` `{"regions":["fra1"]}` co-lokaliserar funktionerna med Neon → produktsida 2,3 s→**0,19 s**, /produkter 1,57→0,39 s (verifierat via x-vercel-id `arn1::fra1`). **Lärdom: håll Vercel-region = Neon-region.** (2) **Navigeringskänsla**: `loading.tsx`-skelett för produkt/utforska/marknad/sets/dashboard/samling (fanns INGA → klick frös tills servern svarade). (3) **Falska "butiker" i filtret**: `NON_STORE_RETAILER_NAMES` (Pokémon TCG API, TCGdex API, Mock-datakälla) exkluderas ur butiksfiltret + visade offers (Cardmarket/Tradera = riktiga marknadsplatser, behålls). (4) **Felmatchade sealed-länkar rensade på Neon**: 13 butiks-offers (Shinycards→fel set, Speltrollet→samlarpärm m.m. via `clean-mismatched-offers.ts` DELETE=1) + 2 CM-offers (Chinese 151 → "151 Booster") raderade. CM-sealed mestadels rätt (bara 2/1546 fel; sök-URL-offers redan dolda). (5) **Tradera-länk på ALLA sealed**: produktsidan visar "Sök på Tradera" (`traderaSearchUrlSpecific(titel, kategori)`) när direkt Tradera-annons saknas (reserv utöver `isDirectOfferUrl`). (6) Skanner-fångst 900→1280 px @ q0.85 för tydligare OCR. **Neon-åtkomst för engångsskript**: inline `DATABASE_URL='<neon-pooled>' npx tsx scripts/x.ts` funkar (tsx laddar inte .env över en redan satt var; verifiera via `current_database()` = `neondb` vs lokal `pokefinds`); URL finns i Neon-konsolen (pooled `-pooler`-host). tsc grön; allt deployat + pushat. **Kvar (ev.)**: /marknad fortf. ~1,5 s (tyngre marknadsfrågor — kan parallelliseras/begränsas); Neon free-tier cold-start ~3–4 s efter inaktivitet (Neon-betald = always-on).

- [x] **Jobben timeoutade → samtidighet + fejk-boxar + graf 2026-06-17**: Användarfeedback (utdaterad data, "köpt för 2 dagar sen visas än"). **ROTORSAK (auto-uppdatering körde EJ klart)**: de dagliga GitHub Actions-jobben (`cardmarket-refresh`, `tradera-sweep`, även `scrape-all`/`restock-watch`) startade på schema men **timeoutade/cancellades** vid 20–30-min-taket — de gjorde ~18k **sekventiella** DB-skrivningar från en US-runner mot Neon i Frankfurt (~100 ms/tur). cardmarket-loggen visade Neon-anslutningsdropp (`57P01 terminating connection`) 9 min in, sedan malande tills den dödades → priser/Tradera-annonser bara delvis uppdaterade. **FIX**: (1) ny `src/lib/concurrency.ts` `mapPool(items, concurrency, fn)` — bunden samtidighet. (2) `cardmarket-refresh.ts` + `tradera-sweep.ts`: separerade rate-limitad API-hämtning från DB-skrivning, kör skrivningarna med `mapPool(…, 8)` (singlar/sealed/matchning/skrivning/expiry). Singel-API-hämtningen (~1000 sidor, latensbunden ~38 min) körs nu OCKSÅ parallellt (`API_CONCURRENCY=4`, varje task sover `throttle×4` → aggregerad takt ≤ ~273/min, under 300/min-kvoten oavsett latens). **Verifierat på prod** (manuell workflow_dispatch): körde KLART (success) — 18 815 singlar + 1 488 sealed uppdaterade, 1190 API-anrop — där gamla körningar cancellades vid 30m30s. (3) `src/lib/db.ts`: opt-in `DB_POOL`-env → `connection_limit`+`pool_timeout` så samtidigheten inte slår i Prisma-poolen (webb-appen sätter den inte → oförändrad). (4) Alla 4 workflows: `DB_POOL=10` + timeout 30→50 / 20→40 (GitHub fakturerar faktisk körtid, så högre tak är gratis när jobbet blir snabbare). Runner-loopen (`scrapers/runner.ts`) lämnad sekventiell med flit — `bestPriceThisRun` (billigast vinner) + restock-event-dedup per dubblettprodukt gör naiv parallellisering osäker (dubbla alert-mejl). **#6 Fejk-boxar**: `scripts/remove-fabricated-boxes.ts` (dry-run default, APPLY=1) raderade 6 påhittade booster boxes för .5-specialset som ALDRIG haft box (Prismatic Evolutions ×2, Crown Zenith, Paldean Fates, Shrouded Fable, 151) — vakt: bara om BOOSTER_BOX + ingen CM `idProduct`-länk. Behöll riktiga "151 Booster Display (36)". **Graf**: konstant uttoning efter markören (hårt steg 1→0,22, platt till idag — inte gradient som bleknar bort) + nytt **1V** (7-dagars) periodpiller (default kvar 3M). **#2 prishistorik**: ingen legitim källa ger äkta daglig historik retroaktivt (CM-graf får ej skrapas; RapidAPI ger bara rullande 7d/30d-snitt) → daglig punkt byggs framåt nu när jobben går klart. tsc + 83/83 + build gröna; `mapPool` självtest i `tests/unit/concurrency.test.ts`.

## Tekniska beslut (VIKTIGA — ändra inte utan skäl)
- **Stack**: Next.js 14 App Router, React 18, TypeScript strict, Tailwind CSS, Prisma, PostgreSQL
- **Växelkurs**: live via `src/lib/exchange-rate.ts` (`getRatesOre()` → Frankfurter, dygnscache, fallback 1150/1050 öre). Anropa i början av en ingest-körning; synkrona pris-funktioner läser `getCachedRatesOre()`. `EUR_SEK`-env pinnar kursen. Hårdkoda ALDRIG 11.50 igen — använd modulen
- **Singelpris**: `Offer.price` på singlar = Cardmarkets engelska **NM-lägsta ("From")** × live-kurs, hämtat från **CardMarket API TCG (RapidAPI Pro)** — `CARDMARKET_RAPIDAPI_*` i .env, fältet `prices.cardmarket.lowest_near_mint` (DECIMAL EUR; bas-fältet är engelska, `_DE`/`_FR`/`_ES`/`_IT` = språk-överstyrningar). Detta ÄR det engelska From-pris som löste det gamla lowPrice-problemet (pokemontcg.io `lowPrice` = all-språk/all-skick-golv som grovt underskattade — använd ALDRIG). Fyll via `scripts/rapidapi-fill-singles.ts` (set-paginering `/pokemon/episodes/{id}/cards`, matcha på `tcgid`=`tcgExternalId`, ~1000 anrop för hela katalogen). Exakt uppslag = `?tcgid={id}` (1 träff). **EXAKT From — INGEN outlier-utjämning** (användaren valde 2026-06-14 att matcha CM 1:1; `OUTLIER_MULT` default 0): priset = `lowest_near_mint` rakt av, även när CM:s billigaste annons är en enstaka feldyr/graderad (t.ex. Ampharos €350 = 3826 kr fast trend bara €68 — det är CM:s "From" och ska matcha). Fallback till `30d_average` BARA när `lowest_near_mint` saknas. Rubrik = "Lägsta pris · NM engelska (Cardmarket)". **GRAFEN/historiken är OFÖRÄNDRAD** = CM trendPrice (`PriceObservation`/`PriceSnapshot`) — headline (From) kan avvika kraftigt från kurvan (trend) på outlier-kort, medvetet. **DURABILITET + AUTO**: `runner.ts` låter inte trend-källan (Pokémon TCG API/TCGdex) skriva över singel-offer-priset; istället **auto-uppdateras From dagligen** av `src/jobs/cardmarket-refresh.ts` (`runCardmarketRefresh()`) — schemalagt en gång/dygn (BullMQ `0 5 * * *` + once-per-day-vakt i worker-fallbacken OCH instrumentation-ticken; egen 3000/dygn-kvot tål inte 8h-tickens 3×/dygn). Sealed-pris = CM `lowest` exakt via samma modul + `scripts/rapidapi-fill-sealed.ts` (matchnings-vakter behålls: boosterbox kräver "booster" i API-namn, poäng ≥0,55, butik-cross-check ×2.5 — men INGEN pris-utjämning)
- **Samlingsvärde**: live via `computeCollectionValue`/`valueCollectionItems` (`src/services/collection.ts`) → `getCardValues`/`getProductValues` (`src/services/products.ts`) = produktens lägsta pris (singel = CM-trend, sealed = butik) × live-kurs. Faller tillbaka på lagrat `estimatedValue` (ögonblicksbild vid tillägg) när live saknas. Skannade kandidater visar samma värde via `estimateCardValue`
- **AI-gradering**: adaptermönster i `src/services/grading/` (`GradingAdapter` + mock + Claude vision). Plan→modell: FREE = `GRADING_MODEL_FREE` (Haiku 4.5, max `GRADING_FREE_DAILY_LIMIT`/dygn), PREMIUM = `GRADING_MODEL_PREMIUM` (Sonnet 4.6). `GRADING_PROVIDER=mock` i dev. Strukturerat svar via tvingat verktyg (`report_grade`). Det är en UPPSKATTNING, aldrig en officiell PSA/BGS-grad — UI:t är tydligt med det
- **PWA/app**: installerbar via `public/manifest.json` + `public/sw.js` (registreras i prod av `src/components/pwa-register.tsx`). Vägen till app-butiker senare = Capacitor-wrapper runt samma Next-app (ingen UI-omskrivning)
- **CM-länkar = exakt slug med `?language=1`** (+ `&minCondition=2` på singlar): visa ALDRIG en bar `prices.pokemontcg.io/cardmarket/{id}`-redirect (302:n strippar language=1). Lös den till `cardmarket.com/.../Singles/...?language=1` via `resolve-cm-urls.ts` (streaming-pool, resumerbar). `isDirectOfferUrl()` döljer olösta redirects; `runner.ts` bevarar lösta slug-länkar framför inkommande redirects (annars skriver 8h-jobbet över dem). **Near Mint**: singel-länkar har även `&minCondition=2` (=NM och bättre, motsvarar API:ts `lowest_near_mint`) via `withNearMint()` — idempotent, redirecten bevarar param. Sealed: INGET minCondition (inget skick)
- **Designtokens**: neutral charcoal-yta + turkos signaturaccent (`holo.cyan` = `#2dd4bf`). Allt färgsätts via tokens i `tailwind.config.ts` — undvik hårdkodade hex/`*-blue-*`-klasser i komponenter så att tema förblir centralt
- **Offers = endast direkta länkar**: visa aldrig sök-/bläddringslänkar som offers. `isDirectOfferUrl()` vaktar både UI och prisstatistik. Butiksfilter kräver IN_STOCK + direkt länk. Direkta länkar UTAN pris visas ändå (pris "–")
- **TCG-import paginering**: använd ALDRIG `orderBy=number` i `fetchTcgCardsForSet` — pokemontcg.io:s string-sort tappar kort mellan sidor. Set kan ha >250 kort (totalCount), paginera stabilt utan orderBy
- **Auth**: NextAuth v4 med Credentials provider + JWT-sessioner. RBAC via `role` på User (USER/MODERATOR/ADMIN/SUPERADMIN)
- **DB**: Användarens lokala PostgreSQL 18 (tjänst `postgresql-x64-18`, installerad i `D:\Website downloads\Relational Databases`). Databas `pokefinds`, user `postgres`, lösenord `pokefinds-local` (återställdes 2026-06-11 via temporär trust-auth). Docker behövs INTE — docker-compose finns kvar som alternativ. `DATABASE_URL` redan satt i .env
- **Cache/queue**: Redis valfri — koden degraderar graciöst utan Redis (in-memory fallback i `src/lib/queue.ts`)
- **Charts**: recharts
- **E-post**: nodemailer, console/JSON-transport i dev (`EMAIL_MODE=console`), SMTP i prod
- **Validering**: Zod överallt på API-gränser
- **Scrapers**: Adapter-mönster i `src/scrapers/`. Endast mock-adapter aktiv. Riktiga adapters MÅSTE respektera robots.txt, rate limits, tydlig user-agent. Ingen captcha/login-bypass. Rå data sparas i PriceObservation.rawData
- **Skanning**: `src/services/scanner/` — OCR-adapter-interface med mock. Koppla riktig vision-API via `OCR_PROVIDER` env
- **Priser**: lagras i öre (integer) för SEK, `currency`-fält. Visa via `formatPrice()` i `src/lib/format.ts`

## Kommandon
```bash
# Postgres körs redan som Windows-tjänst (postgresql-x64-18) — ingen Docker behövs
npm install                     # (--legacy-peer-deps vid peer-konflikt)
npx prisma migrate dev          # migrera
npx prisma db seed              # seeda
npm run dev                     # dev-server på :3000
npm test                        # vitest (71 tester, gröna)
npm run test:e2e                # playwright (kräver seedad DB)
```

## Verifierat 2026-06-11
- Migration + seed körda mot lokal Postgres ✅
- Alla nyckelsidor svarar 200 (/, /produkter, /produkter/[slug], /marknad, /logga-in, /priser, /community, /sets, /sitemap.xml) ✅
- /dashboard redirectar korrekt till login (307) ✅
- 71/71 unit-tester gröna ✅

## Demo-konton (efter seed)
- admin@pokefinds.se / admin1234 (SUPERADMIN)
- demo@pokefinds.se / demo1234 (USER)

## Mappstruktur
```
src/
  app/           # Next.js App Router (sidor + api routes)
  components/    # UI-komponenter (ui/ = design system, features/ = sammansatta)
  lib/           # db, auth, format, queue, validation, utils
  services/      # affärslogik (products, watchlist, alerts, collection, scanner, community, analytics)
  scrapers/      # adapter-system + mock-adapter
  jobs/          # bakgrundsjobb (scrape, alerts, restock-check)
  emails/        # e-postmallar
  types/         # delade typer
prisma/          # schema + seed
tests/           # unit + e2e
docs/            # all dokumentation
```

## Regler
- All copy på svenska, premium men lekfull ton
- Inga hårdkodade hemligheter
- Priser i öre (int), aldrig float
- Mörkt tema som standard
- GDPR: dataminimering, export, radering måste alltid fungera

## Kända begränsningar / Nästa steg
- Se docs/TODO.md
- Stripe-modul förberedd men avstängd (`STRIPE_ENABLED=false`)
- Web push förberett (service worker) men kräver VAPID-nycklar
- Endast mock-datakälla aktiv — riktiga adapters läggs i src/scrapers/adapters/
