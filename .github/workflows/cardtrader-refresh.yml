name: CardTrader-varianter (dagligen)

# Reverse holo, Master Ball / Poké Ball och 1st Edition prissätts av CardTraders
# marknadsplats — den enda källa vi har som prissätter PER TRYCKNING (flaggan
# sitter på annonsen, inte på produkten). Utan det här jobbet står de priserna
# still: importerna kördes manuellt när de byggdes, och prishistoriken byggs
# FRAMÅT, så en produkt utan daglig punkt får aldrig någon graf.
#
# Körs 05:00 UTC, långt från cardmarket-refresh (13:00) och hot-card (21:00) —
# de delar databas och skulle annars slåss om samma anslutningar.
#
# Kostar INGEN RapidAPI-kvot: CardTrader har en egen token och egna endpoints.
# Skripten är idempotenta (upsert per produkt+retailer) och skapar bara nya
# produkter när marknaden fått djup nog — en körning utan nyheter är en ren
# prisuppdatering.
# ── LÄNKAD TILL FÖREGÅENDE JOBB, INTE TILL KLOCKAN (2026-08-05) ──────────────
# Neons nota är VAKEN TID, och varje väckning köper minst 300 s debiterad tid. Fyra
# nattjobb på egen cron (02:00 / 04:00 / 05:00 / 05:30) väckte computen FYRA gånger,
# spritt över ~3 timmar — och eftersom GitHubs cron dessutom släpar 1,5–3,5 h låg de i
# praktiken ännu glesare. Kedjade i stället: ett sammanhängande fönster, en väckning.
# Mellanrummet (runner-kö + checkout + npm ci) är minuter, alltså långt under
# autosuspendens 300 s, så Neon hinner aldrig somna mitt i kedjan.
#
# ⛔ INGEN `conclusion`-grind. Jobben är OBEROENDE av varandra — de delar bara
#    tidsfönster. Gatade vi på `success` hade ett misslyckat Tradera-svep tyst dödat
#    de tre efterföljande jobben, dvs bytt en synlig röd körning mot tre osynligt
#    uteblivna. `types: [completed]` fyrar oavsett utfall.
# ⛔ `workflows:` MATCHAR PÅ `name:`-FÄLTET i det andra filen, inte på filnamnet.
#    Byter du namn på uppströmsjobbet bryts kedjan TYST och det här jobbet slutar
#    köras helt. Byt då namnet på båda ställena i samma commit.
on:
  workflow_run:
    workflows: ["Tradera-svep (dagligen)"]
    types: [completed]
  workflow_dispatch: {}

# Minsta möjliga rättighet. INGET jobb i det här repot skriver till GitHub — de läser
# koden och skriver till Neon. Utan blocket får GITHUB_TOKEN repots STANDARD-rättigheter,
# vilket kan vara skrivande, och en komprometterad beroendekedja hade då kunnat pusha.
# ⛔ Inkommande workflow_dispatch (restock-pingern) påverkas INTE: den auktoriseras av
# ANROPARENS token, inte av det här blocket.
permissions:
  contents: read

concurrency:
  group: cardtrader-refresh
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      CARDTRADER_TOKEN: ${{ secrets.CARDTRADER_TOKEN }}
      EUR_SEK: ${{ secrets.EUR_SEK }}
      APPLY: "1"
    steps:
      # ── ⛔ FAIL-OPEN VAR FEL JUST HÄR (rättat 2026-08-17) ──────────────────────
      # Steget satte förut `ready=false` när en secret saknades, alla efterföljande
      # steg blev `skipped` och körningen rapporterade SUCCESS. `CARDTRADER_TOKEN`
      # finns inte bland repots secrets ⇒ **13 gröna körningar i rad utförde noll
      # arbete** (bekräftat i körning 31925641206: varje substantiellt steg
      # `skipped`, jobbet `success`), och 26 393 CardTrader-offers stod kvar på
      # `lastSeenAt = 2026-08-03`. Det upptäcktes av en ANVÄNDARE som saknade
      # reverse holo-kort, inte av oss.
      #
      # Ett jobb vars ENDA uppgift är att hämta färsk data får aldrig rapportera
      # framgång när det inte hämtat något. Tyst grönt är värre än rött: rött syns i
      # körningslistan, grönt betyder "kontrollerat och i sin ordning".
      # (Den tysta varianten hör hemma i jobb som är MENINGSFULLA utan sitt förkrav
      # — de vi skrev innan Neon fanns. Det här är inte ett sådant.)
      #
      # ⛔ EN RÖD KÖRNING BRYTER INTE NATTKEDJAN. `tradera-sold-sync.yml` länkar hit
      # med `types: [completed]`, som fyrar OAVSETT utfall — alltså ingen
      # `conclusion`-grind, precis som CLAUDE.md kräver. Vaktat av
      # `tests/unit/cron-chain-sync.test.ts`.
      - name: Förkrav — DATABASE_URL och CARDTRADER_TOKEN måste vara satta
        run: |
          missing=""
          if [ -z "${DATABASE_URL}" ]; then missing="${missing} DATABASE_URL"; fi
          if [ -z "${CARDTRADER_TOKEN}" ]; then missing="${missing} CARDTRADER_TOKEN"; fi
          if [ -n "${missing}" ]; then
            echo "::error::Saknade secrets:${missing} — jobbet kan inte hämta något och rapporterar RÖTT i stället för tyst grönt. CardTrader-priserna står still tills secreten finns."
            exit 1
          fi
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: "20"
          cache: npm
      - run: npm ci --legacy-peer-deps
      - run: npx prisma generate

      # Ordningen är den billigaste först: bollarna och 1st Edition rör ett
      # tiotal expansioner var, reverse-passet går igenom hela katalogen (~10 min).
      - name: Master Ball / Poké Ball reverse holo
        run: npx tsx scripts/import-cardtrader-ball-reverse.ts

      - name: 1st Edition (WOTC-seten)
        run: npx tsx scripts/import-cardtrader-first-edition.ts

      # ⛔ TIDSBUDGETEN ÄR `MINUTES`, INTE `SETS` (rättat 2026-09-03).
      #
      # `SETS` var en tidsbudget uttryckt i FEL ENHET. Passet är sekventiellt — en
      # Prisma-rundtur per kort mot Neon/Frankfurt från en US-runner — och hur lång tid
      # ett set tar beror på hur många kort det har och hur många TCGdex-slagningar det
      # kräver. Listan ROTERAR dessutom (stalast först), så SAMMA tal är olika lång tid
      # varje natt: 110 set tog **44,0 min** 09-02 och var inte klara efter **52 min**
      # 09-03, då `timeout-minutes: 60` dödade körningen mitt i set 87
      # (33730849109). Samma sak 08-25 och 08-26. Ett handtrimmat setantal kan alltså
      # inte hålla marginalen — den äts upp så fort rotationen råkar lägga stora set först.
      #
      # En DÖDAD körning är värre än en kortare: den når aldrig
      # `recomputeProductPriceCache()`, så nattens nya reverse-produkter blir stående med
      # `lowestPriceOre = NULL` och göms ur katalogen — men ligger kvar i sitemapen.
      # `MINUTES` stoppar i stället set-loopen FRIVILLIGT och kör omräkningen. Svansen är
      # inte förlorad: ordningen är STALAST FÖRST (b6faea6), så det som hoppades över i
      # natt ligger överst i morgon — rotationen är garanterad utan markör, migration
      # eller extra DB-läsning.
      #
      # 44 min är satt mot taket på 60: de tidigare stegen (npm ci + de två passen ovan)
      # tog 6–8 min de senaste nätterna, gränsen prövas FÖRE ett set så det sista får
      # köra klart (~1–2 min), och omräkningen tar sekunder. Marginal ~6 min.
      # ⛔ Höj inte `timeout-minutes` i stället. Neon debiteras på VAKEN TID och det här
      #    jobbet är redan flottans dyraste enskilda fönster.
      # ⛔ `SETS` är kvar som TAK, inte som budget: på en snabb natt slutar körningen
      #    som förut i stället för att köpa extra vaken tid för att beta av fler set.
      #    Höj den bara med ett kostnadsbesked till ägaren.
      - name: Reverse holo (stalaste seten först — MINUTES är budgeten)
        env:
          SETS: "110"
          MINUTES: "44"
        run: npx tsx scripts/import-cardtrader-reverse.ts
